services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: squared
      POSTGRES_PASSWORD: squared
      POSTGRES_DB: squared
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U squared"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
